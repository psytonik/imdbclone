'use client';

import React from 'react';

const Loading = () => {
	return (
		<div className="flex justify-center">
			<img src="/spinner.svg" alt="loading..." className="mt-10 h-50"/>
		</div>
	);
};

export default Loading;
